export function Footer() {
  return (
    <footer className="border-t border-ink-100 bg-white/70">
      <div className="mx-auto flex w-full max-w-6xl flex-col gap-2 px-6 py-6 text-xs text-ink-500 md:flex-row md:items-center md:justify-between">
        <span>Indicative only. Not investment advice.</span>
        <span>You must be 18+ (UK) to purchase alcohol.</span>
      </div>
    </footer>
  );
}
